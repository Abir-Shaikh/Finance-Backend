# Finance-Backend
Spring Boot REST API with role-based access control for managing financial records — includes user management, CRUD operations, dashboard analytics, and JWT-less Basic Auth security.
